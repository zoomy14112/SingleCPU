## 单周期



## 流水线

### 2-bit predictor

### Return Address Stack

上述两位分支预测器并不能解决 `jalr` 引发的控制冒险，遇到 `jalr` 则必定阻塞两个时钟周期。为此，实现了 Return Address Stack（返回地址栈）以解决这个问题。

从高级语言的角度来看，常有两种情况涉及到 `jalr`：函数调用与函数返回。函数调用时，若目标地址超出 `jal` 的跳转范围，则需使用 `auipc` 配合 `jalr` 跳转；此时暂存跳转地址的寄存器未知且随机，难以预测。函数返回时，目标地址即为函数返回地址，而函数调用时一定会计算返回地址并存储在 `x1` 里，这一点可以预测。函数的调用与返回形成 LIFO 的结构，所以我们可以开一个存储返回地址的栈，发生函数调用时把返回地址压入栈中，发生函数返回时把栈顶弹出，理想情况下每次弹出的栈顶都是当前返回地址。由于取栈顶的过程可以在 IF 阶段完成，取到 `jalr` 指令后可立即查询到其返回值，`jalr x0,0(x1)` 指令不再阻断流水线。

即使维护一个返回地址栈，也不代表 `jalr` 一定会预测成功，所以仍需在 EX 阶段检测预测是否正确。这里采取的方法是比较 `ALUout` 与 `pc_ID` 是否相同，